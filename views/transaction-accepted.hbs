<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            margin: 0;
            padding: 0;
            text-align: center;
            height: 100vh;
        }
        .container {
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            padding: 20px;
            width: 80%;
            max-width: 600px;
            margin: 30px auto;
        }
        h1 {
            color: #333;
            font-size: 24px;
        }
        p {
            color: #555;
            font-size: 16px;
        }
        .info {
            margin-top: 20px;
            padding: 10px;
            background-color: #e1e1e1;
            border-radius: 5px;
        }
        .info strong {
            font-weight: bold;
        }
        .footer {
            color: #777;
            font-size: 12px;
            margin-top: 20px;
        }
        .notBuy {
            color: #6200ee;
            margin-top: 20px;
        }
        .button {
            color: #fff;
            background: #4a148c;
            padding-top: 10px;
            padding-right: 20px;
            padding-bottom: 10px;
            padding-left: 20px;
            font-family: inherit;
            font-weight: inherit;
            font-size: 0.7rem;
            line-height: 1em;
            border-radius: 30px;
            border: 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Detalle de Transacción</h1>
        <h2 class="notBuy">NotBuy</h2>
        <div class="info">
            <p><strong>Monto:</strong> {{amount}}</p>
            <p><strong>Estado:</strong> {{status}}</p>
            <p><strong>Orden de Compra:</strong> {{buy_order}}</p>
            <p><strong>Fecha de Transacción:</strong> {{transaction_date}}</p>
        </div>
    </div>

    <div id="redirectionMessage" style="text-align: center; margin-top: 20px;">
        <p>La página se redirigirá en <span id="countdown">5</span> segundos.</p>
        <button class="button" onclick="redirigir()">Redirigir Ahora</button>
    </div>

    <p class="footer">© 2023 NotBuy. Todos los derechos reservados.</p>

     <script>
        // Función para redirigir
        function redirigir() {
            window.location.href = 'http://127.0.0.1:5173/site/plan';
        }

        // Contador regresivo
        let segundosRestantes = 10; 
        const countdownElement = document.getElementById('countdown');
        const countdownInterval = setInterval(function() {
            segundosRestantes--;
            countdownElement.textContent = segundosRestantes;

            if (segundosRestantes === 0) {
                clearInterval(countdownInterval);
                redirigir(); 
            }
        }, 1000); 
    </script>
</body>
</html>